# Product ADRs — tadeumendonca.io

Architecture Decision Records for the **product** — the current stack and architectural vision of the
site. Taken together, these ADRs *are* the architecture documentation: every load-bearing decision, and
the reversed ones kept as **History** (superseded).

Practice, format and lifecycle: the plugin's `/workflow/adr` (MADR, light significance gate,
supersede-never-delete). Methodology decisions (the dev-loop machine) live in the plugin's own library.

**Numbering:** append-only in authoring order. The reading groups below are presentation; a decision's
number is fixed once its file exists. A superseded ADR carries the forward link to the ADR that replaced
it (the replacement does not depend on the superseded one's number).

**Reading order:** [ADR-0001](./0001-lean-by-design-calibrated-to-strategy.md) is the keystone — *lean by
design, calibrated to strategy* — and every other ADR is read through it.

**How a statement that stopped being true is marked** (settled by the #234 sweep, 2026-07-29). *Supersede,
never rewrite* is the rule; it has **three** shapes, and which one applies depends on whether the original
was a decision, a premise, or a passing reference:
- **Strike + discharge** — for a **decision or accepted cost** that was reversed. The reader must still see
  what was decided. `~~old text~~ **→ discharged <date> by ADR-00NN**`.
- **Rewrite + parenthetical** — for an **incidental reference** (a route name, a file, a cross-ADR
  citation) whose only job is to point somewhere. Striking these buries the working pointer under a dead
  one, so the text is corrected and the original noted in a parenthetical.
- **Leave + flag** — where another ADR has already claimed the reference as deliberate history (e.g.
  ADR-0010's carve-out for `/cv`). The text stays; a note says so, because that carve-out is invisible
  from the file that carries the reference.

**Enforcement: none, and deliberately** (#234's open question). A check can assert that a *listed* path
resolves; it cannot know a claim quietly stopped being true — which is every finding in the #234 sweep.
So the library is **audited by reading**, and the moment is **when an ADR is amended**: an amendment is
where one record changes what another asserts, and every one of the thirteen findings originated exactly
there. Same answer, and the same reason, as the root `CLAUDE.md` records for itself (#233).

## Foundations
| ADR | Title | Status |
|---|---|---|
| [0001](./0001-lean-by-design-calibrated-to-strategy.md) | Lean by design — calibrated to the strategic priority | accepted |
| [0002](./0002-fully-static-spa-no-backend.md) | Fully static SPA, no backend | accepted |
| [0003](./0003-trunk-based-single-environment.md) | Trunk-based delivery, single environment | accepted · amended 2026-07-23 (the `critical-reviewer` holds the merge gate) · **amended 2026-07-29** (#217 — ratification is an artifact the reviewer VERIFIES, never a relay it trusts; and **record correction is safe class**, tested by what the correction asserts) · **amended 2026-07-30** (the owner's boundary narrows to three: `iac`/continuity, a change to the dev-loop's own rules, and publishing an article — reader-facing content is safe class; `brand-guardian`/`editor` still dispatched — by persona prose, with no check behind it and no second backstop) · **amended 2026-07-30 again** (the merge is still the deploy; the deploy now **fires on the `bump:` commit** via `paths: VERSION`, the only tree carrying the version it is tagged with — the surface filter moved into a credential-free `gate` job diffing `vN-1..vN`. Accepted cost: `version-main` is now **load-bearing for deployment**; named residual gap: one dropped filter decision in a 3-merge burst) · **amended 2026-07-31** (the 07-30 list is EXACT: *"an amendment that chooses something"* is struck from the boundary column, so an ADR amendment that decides something is safe class unless the ADR amended is itself one that decides how work is decided. Driven by #301 — nine criteria green, and the reviewer correctly refused to merge because resolving the record's own contradiction in its favour would widen its authority) · **amended 2026-07-31 again** (a predicate over `var.environment` is untestable fiction while one environment exists — `s3_force_destroy` read as protection and was **inverted**, arming `force_destroy` on the live-site bucket against the `versioning` line 8 rows below it; ~9,000 versions at stake. The honest-looking fix — renaming to `production` — plans **19 add / 4 change / 19 destroy** and leaves DNS resolving from empty buckets) |

## Content, SEO & OG
| ADR | Title | Status |
|---|---|---|
| [0004](./0004-build-time-render-not-ssr-or-edge.md) | Content and OG resolved at build time — not SSR/edge | accepted |
| [0005](./0005-og-coverage-every-public-url.md) | Every public URL is SEO- and OG-complete | accepted · route enumeration struck 2026-07-29 (#234 — cite `routes.mjs`, do not re-list) · **extended 2026-07-30** by [0041](./0041-per-article-og-cards.md), and **widened 2026-07-31** (#167): on the article route, OG-complete means *distinct* **and dimensioned** — "there is a card" was met while the card unfurled as a small square, because the `og:image:width/height/type/alt` block was gated on the card being the default one |
| [0040](./0040-build-time-mermaid-diagrams.md) | Diagrams are Mermaid in the markdown body, compiled to inline SVG at build time (committed artifact, keyed by normalised source; Playwright not mermaid-cli, forced by ADR-0021's `--ignore-scripts`; failure is a red build, never a render-time fallback) | accepted · amends [0010](./0010-routing-landing-cv-split-redirects.md) (the orientation claim) and [0008](./0008-brutalist-mono-identity.md) (a generated surface) · ~~slice one of two~~ **amended 2026-07-30** (#170 CLOSED — the dev-loop diagram shipped, both diagrams full width; and the "conformance is asserted mechanically" claim is **narrowed**: the assertions check palette MEMBERSHIP and background-equality, never legibility — a diagram drawn in the page's own background colour was live on the apex with invisible edges and every assertion passed) · **its architectural shape is reused by [0041](./0041-per-article-og-cards.md)** (build-time artifact, committed, bidirectional set-equality guard) · **amended 2026-08-08 — its scope sentence is universal and no longer true**: ~~*"a ` ```mermaid ` fence in a long-form markdown body is a diagram's source"*~~ now reads *"the source of a diagram mermaid can draw"*, because [0047](./0047-authored-svg-figures-outside-the-mermaid-pipeline.md) puts one figure outside this pipeline. The mechanism, its guards and its default status are unchanged · **amended 2026-08-09 — its rejection of a checked-in raster is scoped to DIAGRAMS**: both halves of that rejection (a source of truth outside the repo; labels that must be text) are about a drawing, and neither reaches a photograph. [0048](./0048-content-photograph-is-a-captioned-figure.md) **applies** the argument rather than contradicting it — *"an `<img>` makes every label a picture of a word"* is exactly why the Knuth quotation is authored in prose beside its photograph |
| [0041](./0041-per-article-og-cards.md) | Per-article OG cards — one 1200×630 PNG per article per locale, generated at build time and committed; keyed by the article KEY (never the per-locale slug), `ogImage` derived in `buildEditions` after the fact-parity loop (never authored — it is a `FACT_KEYS` member); regenerated wholesale, overflowing titles refused, and a missing **or** orphaned card is a red build | accepted · extends [0005](./0005-og-coverage-every-public-url.md) · same shape as [0040](./0040-build-time-mermaid-diagrams.md) · constrained by [0037](./0037-localized-article-slugs.md) + [0036](./0036-per-locale-urls-prerender-hreflang.md) · **amended twice on 2026-07-31**: the overflow refusal measured one axis while this record claimed it caught the case it missed; and the "readers can tell two links apart" claim is **discharged** (#167 — the card was distinct and simultaneously unfurled as a small square, since the dimension block was gated on it being the *default* card). Also carries #167's decision that the **default** card is named the other way — English keeps the pinned `og-default.png`, the locale suffix is additive |
| [0043](./0043-harness-inventory-derived-from-plugin-repo.md) | The dev-loop inventory on `/architecture` is **derived from the sibling plugin repo** — a committed manifest generated from `tadeumendonca-skills` plus a drift check (missing · orphaned · changed) run in `app` against a second, **tokenless** checkout of that public repo; the build itself never reads it. Also decides what the diagram may assert: `PreToolUse` hooks **deny** (hooks are classed by **event** — the `SessionStart` hooks carry **documents**, ~~a pair~~ **three since `-skills`#156**, and one of them *deletes files*: the class names what a session-start hook cannot STOP, never that it merely watches — amended 2026-08-06), personas only **advise** — a claim about their judgement, not a denial that `quality-assurance` holds a hook-enforced merge seat — and command families **document**; a closed set, unknown values throw. The CI gates deny too and are deliberately **not** manifest rows: they live in this repo, not in the plugin | accepted · same shape as [0040](./0040-build-time-mermaid-diagrams.md) + the decision index (`adr-source.mjs`) · constrained by [0002](./0002-fully-static-spa-no-backend.md) / [0004](./0004-build-time-render-not-ssr-or-edge.md) · applies [0015](./0015-oidc-immutable-subject-least-privilege.md) to a CI path filter (stays **out** of `deploy`'s gate) · **the first cross-repo build dependency in a repo whose architecture is "content ships in the repo"** — accepted costs: the check is **asymmetric** (a plugin PR cannot see this page, so the red lands late on an unrelated author) and it is **not self-triggering**, since no path in this repo can match the event that falsifies it. Motivating evidence: #318's own spec said 19 personas and 3 hooks; the tree says **6** and **4** — a reading dated 2026-08-03, left standing as evidence about that day rather than as a live count (it is **5** and **5** on plugin `main` at 0.4.54) · **amended 2026-08-04** (the plugin's version is resolved at **deploy** time, with a committed `plugin-release.json` as the floor — #329) · **amended 2026-08-04 again** (`marketing-lead` merged into `product-lead` and `new-issue` joined the root commands, so the published inventory is **5 personas** and **2** un-namespaced commands; the dated 19→6 table is left standing as evidence about that day. Adds the guard this exposed: the components test asserted *manifest ⊆ drawing* only, so a **retired** persona left in the fence passed every check — the drift job compares the manifest to the plugin, never the page to the manifest) · **amended 2026-08-08** (the *Related record* pointer is **struck, not repaired**: it cited a `0044-committed-permission-floor-local-overlay-ephemeral.md` that was **never written** — `git log -S … --all --reverse` puts the pointer's **first** appearance in `4e32d6f`, the very commit that created this record, so it was forward-looking and never valid for a minute — and the number now resolves to [0044](./0044-version-parts-deliberate-major-minor.md), a different decision. Repointing at the methodology library's ADR-0004/0008 was rejected as the cheapest-looking and most dangerous fix: neither decides the committed-vs-local-overlay axis, which [#384](https://github.com/tedeuxx/tadeumendonca-io/issues/384) finds still unwritten five days later, and a wrong link that **resolves** is worse than one that 404s. Deleting the line silently was rejected too — it would erase the evidence that a companion record was promised, and it is still owed) |
| [0047](./0047-authored-svg-figures-outside-the-mermaid-pipeline.md) | **A figure mermaid cannot draw is authored outside the [0040](./0040-build-time-mermaid-diagrams.md) pipeline** — a typed ` ```venn ` fence over a **fixed geometry**, painted as inline SVG by a component, with its arrangement **asserted arithmetically** against the authored words of both editions instead of pinned by an entry in `diagrams.json`. Three conditions bound it: the words stay in the markdown body per locale · the arrangement is asserted, with a mutation that must turn the assertion red · it ships in the **prerendered** bytes (verified over HTTP, not via `page.goto`). Recorded as a **bounded convention** rather than a one-off — a future figure extends this path or is not drawn — and that clause is deliberately the cheap thing for the owner to overturn | accepted · **narrows the scope claim of [0040](./0040-build-time-mermaid-diagrams.md)**, which is amended to point here; that mechanism stays the default and a figure mermaid *can* draw authored this way is a defect against it · constrained by [0002](./0002-fully-static-spa-no-backend.md) / [0004](./0004-build-time-render-not-ssr-or-edge.md) · per locale, [0032](./0032-i18n-locale-layer-english-baseline.md) · conforms to [0008](./0008-brutalist-mono-identity.md) (identity tokens, and 0040's background-equality check applies) · spends [0001](./0001-lean-by-design-calibrated-to-strategy.md). **The gate's copy-as-markdown concern was measured and does not hold**: `shareMarkdown.ts` strips only the ` ```adr-index ` fence and deliberately leaves mermaid alone (`shareMarkdown.test.ts:105`), so a `venn` fence travels on **identical terms** to the mermaid fences beside it — [0035](./0035-static-repo-cards-in-longform.md)/#387 unchanged. Accepted costs: **the guarantee is weaker on the one axis 0040 named** — the overflow check uses an **estimated** glyph width where mermaid **measures**, which is why 0040 embeds fonts as `data:` URIs (a fallback font overflows *silently, and only on a machine other than the author's*) · a second authoring path is a fork in a convention, and the bound is a rule, not an enforcement · it ships component JS into every long-form page's chunk, **unmeasured and with no bundle-size gate** · it carries runtime behaviour (an unguarded `ResizeObserver` took down every test rendering the page) · a phone reader **pans** it, and a `venn` fence renders as a code block on GitHub · **amended 2026-08-09** (#415 — the no-third-path bound was tested against a **photograph** and reads as governing **DRAWN** figures: a raster satisfies none of the three conditions, and the shared `DiagramFigure` shell is the wrong shell for it, because `.diagram-canvas` is selected page-wide by a spec that reads `querySelector('svg')` and measures `NaN` on an `<img>`. And the neutral clause ~~*"the copy-as-markdown payload is unaffected, and this is measured, not argued"*~~ is **discharged**: it rested on the premise that no content body embedded a root-relative image, which four photographs ended — `shareMarkdown.ts` now absolutizes image targets to the origin, and the test asserting the old behaviour was inverted. The `venn` fence's own payload behaviour is unchanged) |
| [0048](./0048-content-photograph-is-a-captioned-figure.md) | **A photograph is evidence, not a carrier of words** — a content photograph is a **captioned figure whose load-bearing text is authored beside it**. Three conditions: the words it depicts are authored as prose next to it, and **delivered exactly once** — the Knuth quotation is a blockquote and appears in no caption and ~~repeated verbatim in the `alt`~~ **in no `alt` either**, struck the same day **inside this record's own MR, before it had ever been in force**, the strike kept for the reason [0044](./0044-version-parts-deliberate-major-minor.md) set the precedent and deliberately **not** written as an `## Amendment`, which would publish *this decision has moved* about a decision that never shipped. **The principle had one unapplied corner and it was the one reader it mattered most to:** a sighted reader met the quotation once, in type; a screen-reader user met it **twice back to back**, the blockquote then the byte-identical `alt` — and in the pt edition the second delivery was an English paragraph read by a pt-BR voice, the only English block there. Duplication is *redundancy* for one reader and *two apparent sources* for the other. The `alt` now describes the frame, per edition, which is what condition 2 always asked; the assertion was **inverted, not deleted** — the quotation in no `alt` and no caption, and the two editions' Knuth `alt`s must **differ**, which catches one left in English. Found by the copy lens, not by any check here · `alt` and `caption` are both **required**, are two different strings, and a missing one **throws** · the box is reserved from the committed file's own size, checked against each JPEG's **SOF marker** by a test in `scripts/` (because `tsconfig.json` records a deliberate decision not to depend on `@types/node`). Rendered through the lone-paragraph facade `Markdown.tsx` already uses three times, opt-in by `src`; **not** a ` ```photo ` fence and **not** an `img` handler. **The honest bound, stated as the reason rather than as a caveat: legibility inside a raster is not assertable and never will be** — the assertions prove the page never scrolls sideways at four widths, the image is inside its figure and actually decoded, alt and caption are non-empty and differ per edition, the quotation is delivered **exactly once**, and the **quotation's own text node is inside the viewport at 320 and 390**, because the quotation is prose. Nothing proves the photograph is readable, which is *why* the words sit beside it | accepted · **narrows the scope of [0040](./0040-build-time-mermaid-diagrams.md)** (rejection of a raster is about diagrams) and **two claims of [0047](./0047-authored-svg-figures-outside-the-mermaid-pipeline.md)** (its bound governs *drawn* figures; its measured copy-payload clause is discharged) — both amended to point here · **changes [0035](./0035-static-repo-cards-in-longform.md)/#387's payload**: an image target absolutizes to the **bare origin** while a link keeps `localizePath`, and the asymmetry is pinned by an assertion that fails if the branches are unified · per locale, [0032](./0032-i18n-locale-layer-english-baseline.md) · spends [0001](./0001-lean-by-design-calibrated-to-strategy.md). **It was never a green field**: `.markdown img` already existed (`index.css:519`), so doing nothing would have shipped four unlabelled, unreserved photographs with **every gate green**. Accepted costs: a **third** figure class on one page and the drawn-vs-captured bound is a rule, not an enforcement · the figure treatment is invisible in the markdown (a photograph is a figure by being alone in its paragraph) · four rasters ship **unoptimised**, a build-time optimiser refused against [0001](./0001-lean-by-design-calibrated-to-strategy.md) and the 1 MB total asserted instead · **the assets have no in-repo producer** — the hand crop silently no-opped once and left a stale EXIF `Orientation` tag on correct pixels, now pinned as the **absence of any Exif block** · more component JS into every long-form chunk, still with **no bundle-size gate** |

## Frontend
| ADR | Title | Status |
|---|---|---|
| [0006](./0006-react-vite-typescript.md) | React + Vite + TypeScript | accepted |
| [0007](./0007-tailwind-no-shadcn-own-components.md) | Tailwind, no shadcn — own tokens + components | accepted |
| [0008](./0008-brutalist-mono-identity.md) | Brutalist mono identity (colors, radius-0, visible grid) | accepted · amended 2026-07-23 (the portrait is round — the one carved exception to radius 0) · **amended 2026-07-30** (the identity has a **generated** surface — the [0040](./0040-build-time-mermaid-diagrams.md) diagrams; same rule, wider reach, conformance asserted on the rendered SVG rather than trusted — but see [0040](./0040-build-time-mermaid-diagrams.md)'s 2026-07-30 amendment: those assertions check palette **membership**, never legibility, and a diagram drawn in the canvas colour shipped) |
| [0009](./0009-self-hosted-fonts.md) | Self-hosted fonts (Space Grotesk + JetBrains Mono) | accepted |
| [0010](./0010-routing-landing-cv-split-redirects.md) | Client-side routing + landing/CV split + back-compat redirects | accepted · amended 2026-07-23 (`/ramp-up`, a fourth public surface) · amended 2026-07-24 (CV route `/cv → /me`, "Perfil / Profile" label) · **amended 2026-07-24 again — pre-launch: the `/cv`, `/profile`, `/articles` redirects are DROPPED** (this row advertised them until #234) · amended 2026-07-25 (`/architecture`, a fifth public surface; single English slug, bilingual label + body) · updated 2026-07-26 (its deferred "localized URLs" question is now made in [0036](./0036-per-locale-urls-prerender-hreflang.md); its deferred dual-slug question is resolved for the blog surface in [0037](./0037-localized-article-slugs.md)) · **amended 2026-07-30** (the 2026-07-25 "it cannot drift from them" claim is **narrowed**: `/architecture` now carries a diagram — its first statement about the system rather than a pointer to one — see [0040](./0040-build-time-mermaid-diagrams.md)) · **amended 2026-08-05** (`/library`, a sixth public surface, #166 — one English slug prefixed twice, bilingual label; **chrome around typed data**, following `/portfolio` rather than the markdown surfaces; the first surface added post-launch, so both URLs are permanent from merge. Its slug reasoning is **not** restated here — see [0036](./0036-per-locale-urls-prerender-hreflang.md)'s 2026-08-05 amendment, since this ADR's 2026-07-25 per-surface dual-slug rejection was reopened on the very next surface) · **amended 2026-08-06** (#166 — `/library`'s deferred nav entry and `/ramp-up` cross-link discharged with the first books; the routing contract is unchanged, and markdown now resolves site-internal links to the ACTIVE locale, so an author writes `/library` and a pt page links `/pt/library`) |
| [0012](./0012-snake-case-content-no-mapping.md) | snake_case content/data, no mapping layer | accepted · **amended 2026-07-30** (the casing split — camelCase in frontmatter and the catalog modules, snake_case in `profile.ts`; no-mapping-layer unchanged) |
| [0032](./0032-i18n-locale-layer-english-baseline.md) | i18n — light in-repo locale layer, native auto-detect + toggle (~~English-pinned crawlable baseline~~) | accepted · **Slice 2 SHIPPED as [0036](./0036-per-locale-urls-prerender-hreflang.md)** — the English-pinned prerender clause and every consequence hanging off it are retired; struck through in-file 2026-07-29 (#234). What 0032 still owns: the locale layer itself. |
| [0036](./0036-per-locale-urls-prerender-hreflang.md) | Language is addressable — symmetric `/pt` · `/en` URL prefixes, per-locale prerender, hreflang + OG-per-locale (x-default = English; no edge, `iac/` untouched) | accepted · blog-slug clause revised by [0037](./0037-localized-article-slugs.md) (localized article slugs; per-locale URLs/prerender/hreflang otherwise stand) · amended 2026-07-27 (x-default is the **prefixed** English canonical on every route but the root; invariant: a URL may be advertised only if the build prerenders it) · amended 2026-07-28 (the reader's own edition is **offered**, never redirected to — second key `locale-suggestion-dismissed`; invariant: the prerender is not a visitor) · **amended 2026-08-05** (#166 — the "NOT dual localized slugs" clause is **reaffirmed, not revised**, and the reason it kept being reopened is recorded: **language-pinning** is delivered by the *prefix* and was already shipped, **slug readability** only by a *pair*, and four openings fused the two. The fourth cost a full analysis and a built slice, reverted before merge. Names the cost that a static pair is **booby-trapped, not absent** — `localizeArticlePath` passes non-article paths through with a green test asserting it, so the next attempt dead-ends the toggle on a green suite) |
| [0035](./0035-static-repo-cards-in-longform.md) | Curated repos as static cards in long-form — lone-URL facade + leaf-bilingual field, no third-party fetch | accepted |
| [0037](./0037-localized-article-slugs.md) | Article slugs are localized — a locale-matched slug per edition (identity = filename key, not a URL; blog surface only; `iac/` untouched) | accepted · clarified 2026-07-27 by [0036](./0036-per-locale-urls-prerender-hreflang.md)'s amendment (its "x-default = the English slug" means the **prefixed** English slug) · **cited 2026-07-30 by [0041](./0041-per-article-og-cards.md)** (its editable per-locale slug is why an OG card is keyed by the article KEY and not by a URL) |
| [0045](./0045-document-title-leads-with-section-label.md) | A document title is an **address**, not the page's argument: it LEADS with the section's `nav.*` label — the exact word the reader clicked — with anything after it optional and secondary, and it is sourced from its own `title` leaf, never from a `heading`. Four exceptions with their reasons (`/` is the site · `/blog/:slug` is a document whose own name is its address · `/me` composes from data · **a title that already complies is not rewritten**, since that re-pins an OG card for no reader gain). The rule is derived from the catalog rather than listed, and asserted on the served bytes with `og:title` equal to `<title>` | accepted · constrained by [0041](./0041-per-article-og-cards.md) (the `og:title` half is pinned on first fetch — the single property behind three of the four exceptions) and [0036](./0036-per-locale-urls-prerender-hreflang.md) (per locale, both editions) · applies to the surfaces of [0010](./0010-routing-landing-cv-split-redirects.md) · accepted costs: the rule is a **prefix test** and cannot judge the tail, `/me` sits outside the derived check, and `nav.*` becomes load-bearing for SEO — a nav-label edit now moves five document titles **and all five of the corresponding `og:title`s**, across ten prerendered URLs, every one pinned per URL on first fetch (`og:title` is emitted unconditionally at `useDocumentHead.ts:167`, so no route's title is the tab alone) |

## Infrastructure
| ADR | Title | Status |
|---|---|---|
| [0013](./0013-s3-cloudfront-hosting.md) | S3 + CloudFront (PriceClass_100) + CloudFront Function URL-rewrite | accepted |
| [0014](./0014-terraform-cloud-pipeline-only.md) | Terraform + Terraform Cloud, pipeline-only apply/destroy | accepted · **amended 2026-08-03** (the "no irreversible cloud mutation in the inner loop" claim is **narrowed**: it holds for Terraform, not for the account — the trust root is created by hand, see [0042](./0042-trust-root-bootstrapped-out-of-band.md)) |
| [0015](./0015-oidc-immutable-subject-least-privilege.md) | GitHub OIDC deploy roles — immutable subject, least-privilege | accepted · the provider its trust points at is bootstrapped out of band ([0042](./0042-trust-root-bootstrapped-out-of-band.md)) |
| [0016](./0016-custom-email-via-icloud.md) | Custom email via iCloud (MX/DKIM/SPF) | accepted |
| [0017](./0017-no-waf-no-cmk-ssm-string-only.md) | No WAF, no CMK, SSM String-only config bus | accepted · its "(pending)" app-security ADR is [0021](./0021-application-security-posture.md), accepted the same day (#234) |
| [0042](./0042-trust-root-bootstrapped-out-of-band.md) | The trust root is bootstrapped out of band — Terraform manages the fed deploy role and never the OIDC provider, the infra role, or its policy (two independent reasons: the bootstrap circularity, which is mechanical, and self-escalation, which is permanent); the exception is to 0014's *principle*, not to `terraform apply` | accepted · **amends [0014](./0014-terraform-cloud-pipeline-only.md)** · relies on [0015](./0015-oidc-immutable-subject-least-privilege.md) · accepted cost: nothing mechanical enforces removing the bootstrap credentials, and the hand path reopens on every `tadeumendonca-iac-deploy` revision |

## SDLC, quality & security
| ADR | Title | Status |
|---|---|---|
| [0018](./0018-ci-gates-e2e-on-pr-coverage.md) | CI gates (build-test + infra-plan + lint-workflows) — E2E on the PR, unit coverage ≥85% | accepted · amended 2026-07-23 (`lint-workflows`; why a filtered workflow cannot be required) · amended 2026-07-28 (gate ownership is by what a file IS, not its directory: a behaviour-bearing file under `iac/` is unit-gated by `build-test`) |
| [0019](./0019-complete-functional-regression.md) | Complete automated functional regression (E2E now; API when a backend exists) | accepted |
| [0020](./0020-sonarcloud-quality-gate.md) | SonarCloud quality gate (SAST + coverage + smells), blocking | accepted · amended 2026-07-28 (scope widened to `apps/fed/scripts` — the build tooling was outside the index entirely; `iac/` + workflows remain the open gap) |
| [0021](./0021-application-security-posture.md) | Security posture — minimal by no-backend; Sonar SAST + package-vulnerability scanning | accepted · amended 2026-07-27 (third lever: supply-chain execution control — `npm ci --ignore-scripts` in every CI install) |
| [0022](./0022-numeric-semver-auto-release.md) | Numeric SemVer, auto-bump + release on merge | accepted · its "no gate of its own" cost closed 2026-07-23 by `lint-workflows` ([0018](./0018-ci-gates-e2e-on-pr-coverage.md), #234) · **`version-main` became load-bearing for DEPLOYMENT 2026-07-30** — the deploy now fires on the `bump:` commit, so a wedged bump stops the site shipping, not just tagging ([0003](./0003-trunk-based-single-environment.md)'s second 2026-07-30 amendment) |
| [0023](./0023-observability-static-site.md) | Observability = GA + client error surface + prerender smoke | accepted · refined by [0033](./0033-ga4-consent-gated-analytics.md) (GA4, consent gate) · amended 2026-07-29 (#237 — a post-deploy assertion belongs to the workflow that performs the change it observes; the edge-rewrite check moved to `infra-apply`) |
| [0033](./0033-ga4-consent-gated-analytics.md) | GA4, consent-gated — hard opt-in gate, not Consent Mode v2 | accepted |
| [0039](./0039-share-campaign-tagging.md) | Reader shares are campaign-tagged — three immutable UTM parameters (`utm_medium=social`, `utm_campaign=reader-share`), no `utm_content`; arrival URL captured at module load so the consent gate does not cost the attribution | accepted |
| [0044](./0044-version-parts-deliberate-major-minor.md) | **Version digits carry content meaning** — major and minor are a deliberate dispatch, patch stays automatic. `deploy`'s `workflow_dispatch` carries a `part` input, so a non-patch tag is producible for the first time: every tag in the repository is `0.1.N` (`git tag --list \| grep -cv '^v0\.1\.'` → 0). **MAJOR** = a milestone with a **configured release plan**, **from `1.0.0` onward** — a cohort settled when it opens; a publication may be **inside** one, and publishing is never by itself what closes it. **MINOR** = one publication. **PATCH** = a correction to published content or an app bugfix, and it **stays automatic on every merge**. **`1.0.0` is the architecture publication** — the boundary cut, owing no cohort, with the release-plan discipline beginning *after* it. Explicitly does **not** claim "reader-invisible work bumps nothing" — the owner has settled that model but this mechanism does not implement it, and recording an unenforced rule is the false-mechanism defect this library exists to catch. Rejects the per-PR `semver:` label (a judgement on every PR to buy a correct digit on **fewer than 1 in 30**, nearer 1 in 60 — see the record for the derivation — and the bugfix/refactor split it most needs is **not derivable**) and a separate `release.yml` (here the bump commit **is** the deployed tree, so a second workflow would tag without shipping) | accepted · **discharges nothing in [0022](./0022-numeric-semver-auto-release.md)**, which decided the automatic patch and is left intact · constrained by [0003](./0003-trunk-based-single-environment.md) (the merge is the deploy — the reason option 4 fails) · serves [0038](./0038-content-distribution-linkedin-and-x.md) · cost-tested against [0001](./0001-lean-by-design-calibrated-to-strategy.md). Motivating measurement, dated 2026-08-08: **22 `product` issues created and 21 closed** in the seven days from 2026-08-01 — intake and delivery run at the same rate by design, so a MAJOR defined as "when the backlog is at N %" can never fire and only a **fixed cohort** can. Two clauses were struck inside the record's own MR, before it had ever been in force, and the strikes are kept: ~~content can never contribute to a MAJOR~~ (never decided — a scoping rule about a progress metric, generalised into a prohibition) and ~~`1.0.0` is blocked on an artifact that does not exist~~ (the accepted cost of the option the owner did not take). Accepted costs that stand: nothing enforces the meaning (no check can see a release plan), the patch digit still counts merges rather than corrections, a wrong cut is **unfixable forward**, and the model's central mechanism is unexercised on the very cut that advertises it |

## Presence / cross-surface
| ADR | Title | Status |
|---|---|---|
| [0024](./0024-profile-canonical-cv-cross-surface.md) | `profile.ts` as canonical structured CV; cross-surface coherence (LinkedIn, catalog, X, newsletter) | accepted · amended 2026-07-23 (bilingual CV, English canonical) · amended 2026-07-23 (derived facts are never restated in prose) · amended 2026-07-28 (Canva CV **logically retired** — kept, but no longer a surface; `/me` + `/cv.pdf` are the only CV editions. Decision deferred by [0034](./0034-build-time-cv-pdf-static-artifact.md); nothing links to it, so no follow-up) |
| [0034](./0034-build-time-cv-pdf-static-artifact.md) | Downloadable CV = build-time PDF printed from `/me` to a static asset | accepted · amended 2026-07-26 (PDF source route string `/me → /en/me` per [0036](./0036-per-locale-urls-prerender-hreflang.md)) · amended 2026-07-28 (one-page recruiter edition, #161) · **amended 2026-08-02** (the print palette becomes the page's own dark theme; the budget goes to two pages; the proficiency meters and the Credly badges come back, #317) · its "does not retire Canva" note discharged 2026-07-28 by [0024](./0024-profile-canonical-cv-cross-surface.md)'s amendment (#225) |
| [0038](./0038-content-distribution-linkedin-and-x.md) | Published content is distributed to LinkedIn **and** X in the same batch — medium-adapted copy, canonical URL, mechanics private | accepted · amended 2026-07-27 (privacy clause narrowed: the draft **generator** is repo tooling because it resolves share URLs from the prerendered route list; only its output stays private) · the bare-x-default hazard its amendment describes was fixed at source 2026-07-27 by [0036](./0036-per-locale-urls-prerender-hreflang.md)'s amendment (#200) · amended 2026-08-08 (cadence set at **one article per week**, the pair is its fan-out; baseline measured against `origin/main` at `6e1d66c` — one article, a second published and reverted the same day and now held; `product-lead`'s slower-beat recommendation recorded as overruled; option 4's "revisit if cadence justifies it" trigger explicitly **not** fired) |
| [0046](./0046-video-derived-articles-earn-their-place-by-receipt.md) | **A video-derived article earns its place by receipt, stands without the video, and never runs in a streak** — two gates and one ordering criterion, applied by the owner in about a minute with no intake. **1 · receipts:** name the artifact in these repos that proves the video's claim right or wrong; if naming it takes more than one sentence it is a summary, not an article. **2 · standalone:** delete the video — still an article? If not it is a LinkedIn post with a link and ships there, which is the *correct surface*, not a demotion, and costs nothing extra in the pair [0038](./0038-content-distribution-linkedin-and-x.md) already requires. **3 · order:** by how much of the piece already exists as a published artifact. **Plus the cap — at most one video-derived article between two originals — and its reason is VOICE, not volume:** one reaction reads as engagement, a run of seven reads as a channel that needs other people's names to have something to say. Framed as scheduling the decision had no forcing function and was deferred by three consecutive intakes (#259 · #378 · #405); framed as voice it has a rule. Does **not** decide the angle — every survivor still needs the owner's own thesis | accepted · **constrained by [0038](./0038-content-distribution-linkedin-and-x.md)** and deliberately **not** an amendment to it: 0038 decides distribution and cadence, and its 2026-08-08 amendment says in terms that it is *not an editorial production quota* and that justifying article selection on its own merits *"is a different decision and gets its own record"* · reads through [0001](./0001-lean-by-design-calibrated-to-strategy.md) (the instrument is three questions in a document — no label, no gate, no tooling). Measurement behind the cap, at `origin/main` `56c43e0`: **eight queued video-derived items against one published article** (`git ls-tree … content/blog/` → `my-commitment.{en,pt}.md`). Applied backwards it reproduces the ratified set without anyone's judgement — #207 and the unfiled ontologies item fail test 1, #210 and #378 fail test 2 and are **kept by dropping their videos**, #379 · #339 · #259 pass both. Accepted costs: it drops genuinely interesting items, it converts two commentaries into articles that no longer cite their source, **nothing enforces any of it** (no instrument in this repo observes the *sequence* of publications at all), and **#405's defer sits outside the rule** — held on authority-borrowing cost, re-entry after two of five have shipped |

## History (superseded — reverse-engineered, kept not deleted)
| ADR | Title | Superseded by |
|---|---|---|
| [0025](./0025-superseded-backend-platform.md) | Backend-ful platform (BFF/Lambda · DynamoDB · Cognito · SES · Lambda@Edge) | 0002 |
| [0026](./0026-superseded-lambda-edge-og.md) | Lambda@Edge OG renderer | 0004 |
| [0027](./0027-superseded-backend-link-unfurl.md) | Backend link-unfurl / OG preview cards | 0004 |
| [0028](./0028-superseded-gitflow-two-env.md) | GitFlow, staging + production two-environment | 0003 |
| [0029](./0029-superseded-offline-first-pwa.md) | Offline-first PWA | 0002 |
| [0030](./0030-superseded-monorepo-pwa.md) | Monorepo `tadeumendonca-pwa` consolidation | the `-io` rename + static pivot |
| [0031](./0031-superseded-shared-regional-waf.md) | Shared regional WAF | 0017 |
| [0011](./0011-ui-ptbr-i18n-deferred.md) | UI in pt-BR; i18n deferred | 0032 |

## Known gaps (honest — surfaced by the reverse-engineering, tracked as follow-up Issues)
- ~~**No `sitemap.xml` / `robots.txt`** (ADR-0005)~~ — **RESOLVED** ([#51](https://github.com/tedeuxx/tadeumendonca-io/issues/51)): build emits `sitemap.xml` + ships `robots.txt`; on-page SEO and discovery are both complete.
- ~~**No package-vulnerability scanning** (ADR-0021)~~ — **RESOLVED** ([#52](https://github.com/tedeuxx/tadeumendonca-io/issues/52)): blocking `audit-ci` gate in `build-test` (high/critical prod deps) + Dependabot; the AppSec floor is whole.

New ADRs: copy the plugin's `template.md` → `NNNN-kebab-title.md`, next number in sequence. A reversed
decision becomes `superseded by ADR-XXXX` and links forward — never deleted.
