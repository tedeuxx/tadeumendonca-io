name: app

# PR gate for apps/** — named after the top-level directory it guards, not after what it does to it.
# (Formerly `build-test.yml`. The WORKFLOW was renamed; the terminal JOB is still `build-test`, because
# that name is what branch protection requires — see the aggregator at the bottom of this file.)
#
# trunk-single-env: main IS production, so there is no downstream tier to defer a check to — the full
# regression (Playwright E2E against a preview of the built app) runs HERE, before the merge deploys.
#
# The former 13-step monolith is split into one job per command. What that buys is parallelism and a
# readable graph; what it costs is a fresh runner (and a fresh `npm ci`) per job. That trade is an OPEN
# question in .github/workflows/README.md pending a wall-clock measurement — if the measurement says the
# monolith was faster, this file is the thing to revert, not the naming.
#
# `npm-ci` is a real job with a real purpose, but NOT the one the graph suggests: node_modules cannot
# cross runners. It fails fast on a broken lockfile and it POPULATES the actions/setup-node npm cache
# (~/.npm) that every job below restores from, so the parallel installs are warm rather than cold. Each
# job still runs its own `npm ci --ignore-scripts`. Persisting node_modules as an artifact was considered
# and rejected: this tree (playwright, vite, mermaid) is larger than the download it would save.
on:
  pull_request:
    branches: [main]
  # Kept on push-to-main deliberately, though the design doc draws only the PR edge: SonarCloud needs a
  # main-branch analysis to have a "new code" baseline to compare PRs against. Drop this trigger and the
  # quality gate slowly stops meaning what it says.
  push:
    branches: [main]
    paths:
      - "apps/**"
      - "packages/shared/**"
      - "iac/cloudfront-functions/**" # unit-tested here, not by `iac` (#205)
      - "docs/adr/**" # authored source of the published decision index — see the `test` filter (#318)
      - "VERSION" # a build input — see the `version` filter below
      - "tsconfig.base.json"
      - "package.json"
      - ".github/workflows/app.yml"

# Read-only by default. No job in this file touches AWS, so `id-token: write` appears nowhere.
permissions:
  contents: read

concurrency:
  group: app-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"

jobs:
  # Runs the paths-filter ONCE. No credentials, installs nothing, decides only.
  changes:
    runs-on: ubuntu-latest
    outputs:
      code: ${{ steps.filter.outputs.code }}
      test: ${{ steps.filter.outputs.test }}
      harness: ${{ steps.filter.outputs.harness }}
      version: ${{ steps.filter.outputs.version }}
      citations: ${{ steps.filter.outputs.citations }}
      any: ${{ steps.filter.outputs.code == 'true' || steps.filter.outputs.version == 'true' }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0 # on `push`, dorny diffs against github.event.before and needs the history

      - name: Detect changes
        id: filter
        uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4.0.3
        with:
          filters: |
            # `code` gates eslint/tsc/npm-audit/build-static/playwright — the jobs that actually read
            # application source or produce/exercise a build artifact. Kept narrow on purpose: this used
            # to be the ONLY category, and folding docs/adr/**, CLAUDE.md and deploy.yml into it (as this
            # comment used to explain, one guard at a time) meant an edit to any of those three ran the
            # full gate even though none of eslint/tsc/npm-audit/build/playwright opens them. Split into
            # `code` / `test` / `harness` below so each guard's authored source lights up only the job
            # that actually checks it — surfaced by a real docs-only PR that ran Playwright for a
            # `.gitignore` + `CLAUDE.md` change.
            code:
              - 'apps/**'
              - 'packages/shared/**'
              - 'tsconfig.base.json'
              - 'package.json'
              - '.github/workflows/app.yml'
              # The CloudFront viewer-request function is IaC by path and JavaScript by nature. Its
              # behaviour is unit-tested HERE (apps/fed/scripts/spa-rewrite.test.mjs reads
              # ../../../iac/cloudfront-functions/spa-rewrite.js directly, #205) because `iac` validates
              # Terraform, not behaviour. Without this line an edit to the function alone would skip the
              # only gate that exercises it. NOTE it is ALSO in the `iac` workflow's filter and must stay
              # there: iac/frontend.tf embeds this file via `file()`, so an edit to it is a real Terraform
              # diff that has to be planned and applied. Two gates, two different claims, both required.
              - 'iac/cloudfront-functions/**'

            # `test` is a SUPERSET of `code` (dorny/paths-filter lets one path match more than one
            # category — every `code` pattern is repeated here on purpose, not duplicated by accident):
            # everything `code` needs, PLUS the authored source of two guards that only `vitest` (and,
            # downstream, `sonarqube-scan`, which needs vitest's coverage artifact) actually reads.
            test:
              - 'apps/**'
              - 'packages/shared/**'
              - 'tsconfig.base.json'
              - 'package.json'
              - '.github/workflows/app.yml'
              - 'iac/cloudfront-functions/**'
              # `docs/adr/` is the AUTHORED SOURCE of the decision index published on /architecture:
              # apps/fed/scripts/gen-adrs.mjs compiles it into a committed artifact, and
              # adr-source.test.mjs fails when the two separate. Every other generated artifact here
              # reads from `apps/fed/src/content/`, so its guard is self-triggering; this is the first
              # whose source lives outside `apps/**`.
              #
              # Without this line the guard is correct and NEVER RUNS ON ITS OWN TRIGGER. An ADR-only PR
              # skips `vitest`, `build-test` reports "nothing was verified" and passes — and the page
              # says, in both locales, that a decision added or re-statused either updates it or turns
              # the build red. That claim is only true with this entry. The red would otherwise surface
              # on the next unrelated PR touching apps/**, landing on an author who did not cause it.
              #
              # NOT added to the deploy gate's filter, deliberately: this entry makes an ADR-only PR go
              # red, the author regenerates, and the PR then touches the artifact under `apps/fed/`,
              # which the deploy pathspec already matches. Widening the deploy filter instead would
              # republish on ADR edits that changed no served byte.
              - 'docs/adr/**'
              # `apps/fed/scripts/deploy-workflow.test.mjs` reads `.github/workflows/deploy.yml` directly
              # and asserts the properties that make a version reach the footer: that the bump part is an
              # input rather than the literal `patch`, that a deliberate dispatch still runs the tag/tree
              # assert, and that no downstream job identifies a tree by a moving ref. Without this line
              # that guard is correct and NEVER RUNS ON ITS OWN TRIGGER — a deploy.yml-only PR matches
              # nothing here, `vitest` skips, `build-test` reports "nothing was verified" and passes.
              #
              # NOT added to the deploy gate's filter, for the reason `docs/adr/**` gives above and one
              # sharper: `.github/workflows/deploy.yml` is ALREADY in both of that gate's pathspecs, so
              # there is nothing to widen there.
              - '.github/workflows/deploy.yml'

            # `harness` stands alone, gating ONLY `harness-drift`. `CLAUDE.md` carries a `roster:dispatch`
            # fence naming the subagents an agent may invoke, and `check-harness-drift.mjs` — the ONLY job
            # that reads CLAUDE.md — compares it to the plugin's `agents/`. Nothing else in this file opens
            # CLAUDE.md: not eslint, not tsc, not vitest, not the build. It used to live in `code` (arrived
            # with #353) on the argument that an authored source belongs in the filter that guards it —
            # true, but that filter fanned out to every `code` job, so a CLAUDE.md-only PR ran the full
            # app gate (measured over one month at the time: 11 commits) for a file only harness-drift
            # reads. Split into its own category once that cost showed up on a real docs-only PR.
            #
            # NOT in the deploy gate's filter: `CLAUDE.md` ships no served byte at all, so a deploy
            # triggered by it would republish nothing.
            #
            # THE MANIFEST IS THE SECOND ENTRY, and it was missing until #463. `harness.json` is the OTHER
            # file `check-harness-drift.mjs` compares against the plugin tree — and since #463 it is the
            # source four published claims on /architecture are derived from. Filtered on CLAUDE.md alone,
            # a hand-edit to the manifest plus a matching edit to the drawing agreed with each other,
            # satisfied every assertion in architecture-diagrams.test.mjs, and published a false statement
            # about the plugin with the one job that would have caught it never running. So the filter now
            # matches EITHER authored input to that job: the roster fence in CLAUDE.md, and the committed
            # manifest itself. (It ships a served byte, so it is in `code`/`test` too — but those gates
            # never compare it to the plugin, which is the check that was absent.)
            #
            # THE CHECKER'S OWN CODE IS THE THIRD AND FOURTH ENTRIES, and it was missing until #615.
            # `check-harness-drift.mjs` and the `harness-source.mjs` it imports match `code` and `test`
            # via `apps/**` but matched nothing here — so a checker-only PR ran eslint, tsc, vitest,
            # build and Playwright, and did NOT run the guard it was editing. Without these lines the
            # guard is correct and NEVER RUNS ON ITS OWN TRIGGER, which is the same argument this file
            # already accepted twice in the `test` filter above, for `docs/adr/**` and for
            # `.github/workflows/deploy.yml`.
            #
            # WHAT THIS DOES NOT BUY, stated because the job's own comment below is about a different
            # gap: nothing about the blind window. The event that falsifies the manifest is still a
            # merge in the other repository, no trigger here can match it, and that cost stands
            # unchanged (measured at fourteen days on #615). This closes the narrower gap only — an
            # edit to the comparison logic now runs the comparison.
            harness:
              - 'CLAUDE.md'
              - 'apps/fed/src/content/generated/harness.json'
              - 'apps/fed/scripts/check-harness-drift.mjs'
              - 'apps/fed/scripts/harness-source.mjs'
              #
              # THE CHECKER'S INPUTS ARE THE FIFTH, SIXTH AND SEVENTH ENTRIES, and they were missing
              # until #618. #615 put the checker's own CODE here and stopped there, so the filter covered
              # 2 of the 5 authored inputs this repo hands `check-harness-drift.mjs`.
              # Cited by CLAUSE rather than by line, because a line number rots on the next edit to
              # that file: the checker reads BOTH architecture editions through `pluginLinkTargets`,
              # resolves every target against the checked-out plugin tree in `pluginLinkReport`, and
              # reads `plugin-release.json` through `assertPluginVersion`, whose value `isAhead` then
              # compares against the plugin's own VERSION as `readPluginVersion` reports it.
              # All three matched only `code`/`test` via `apps/**` — and neither of
              # those gates opens the plugin tree at all, which is the comparison that was absent.
              #
              # Without these lines the guard is correct and NEVER RUNS ON ITS OWN TRIGGER — the same
              # argument this file already accepted for `docs/adr/**`, for `.github/workflows/deploy.yml`
              # and, one entry above, for the checker's own code. Concretely: add a link to a plugin file
              # that does not exist in `architecture.en.md` and eslint, tsc, vitest, build and Playwright
              # all run while `harness-drift` does not. #580 existed because three dead cross-repo links
              # were live on /architecture; this is the mechanism that let them get there.
              #
              # WHAT THIS COSTS, on the owner's 2026-09-07 ruling («Alarga, os três entram») with the cost
              # stated in the option he answered: a PR that only edits the architecture page becomes
              # eligible to absorb a `harness-drift` red CAUSED BY A MERGE IN THE PLUGIN REPO, not by that
              # PR. That is the innocent-author cost the job's own comment below already documents,
              # widened to a third surface. It is the same trade #615 declined for `app.yml` — taken here
              # and not there because these three inputs carry a demonstrated failure and `app.yml` a
              # hypothetical one. `app.yml` is STILL not in the filter it defines; that is a separate
              # worth call and it is the owner's.
              #
              # NOTHING GATES THIS LIST. No test in either repo asserts the `harness` filter's contents,
              # so deleting any entry above reddens nothing and this fix is hand-maintained: it drifts the
              # day the checker gains a sixth input. A generated-or-asserted list would close the class
              # permanently rather than instance by instance, and is deliberately not built here.
              - 'apps/fed/src/content/architecture.en.md'
              - 'apps/fed/src/content/architecture.pt.md'
              - 'apps/fed/src/content/generated/plugin-release.json'

            # Split out because VERSION is a BUILD INPUT and nothing else: apps/fed/src/lib/version.ts
            # imports the root VERSION with `?raw` and the footer renders it. A VERSION-only change
            # therefore has to rebuild and re-run E2E, but has nothing for eslint/tsc/vitest/Sonar to
            # look at. This line is also a live gap being closed — VERSION was in NO filter before, so an
            # edit to it changed what a reader sees with no gate run at all.
            version:
              - 'VERSION'

            # `citations` exists because the ADR citation gate (#456,
            # apps/fed/scripts/adr-citations.test.mjs) is the first check here whose SUBJECT is the
            # whole repository. It scans every tracked text file for `ADR-NNNN` and `docs/adr/NNNN-….md`
            # and fails on any that names a record which does not exist — 1,754 citations at the head
            # this landed on, of which 373 live outside `apps/**` in 116 files.
            #
            # `test` already covers the two paths that matter most: `apps/**` (where most citing files
            # are) and `docs/adr/**` (which is where the dominant failure is BORN — a record renamed or
            # deleted breaks every citation to it at once). This category adds the rest of the citing
            # surface, so a citation typed into a Terraform file or a workflow is checked by the PR that
            # typed it rather than by the next unrelated one.
            #
            # It gates `vitest` ONLY — deliberately not `sonarqube-scan`, which stays on `test`. Sonar
            # reads `sonar.sources=src,scripts`; an `iac/`-only PR gives it nothing new to analyse, and
            # fanning the scan out to it would repeat the cost the `code`/`test`/`harness` split was
            # made to stop.
            #
            # RESIDUAL, named rather than papered over: a citation introduced in a tracked file matching
            # NONE of these categories is caught on the next PR that does match, landing on an author
            # who did not cause it. That is the same trade `harness-drift` documents above, and the
            # alternative — matching `**` — would run `vitest` on every PR and make the whole filter
            # decorative.
            citations:
              - 'docs/**'
              - 'iac/**'
              - '.github/**'
              - '*.md'

  # Fails fast on a broken lockfile and warms ~/.npm for every job below. Condition is `test || version`,
  # not `any` (code || version): `test` is a superset of `code` (see the filter above), so this already
  # covers every job below except `harness-drift`, which needs no npm install at all.
  #
  # `citations` is ORed in for a mechanical reason, not a stylistic one (#456): `vitest` declares
  # `needs: npm-ci`, and a SKIPPED dependency skips the dependent. Gating `vitest` on `citations`
  # without gating this job too would leave the citation gate never running on its own trigger — the
  # exact failure the `docs/adr/**` note in the filter above spends a paragraph on.
  npm-ci:
    needs: changes
    if: >-
      needs.changes.outputs.test == 'true' || needs.changes.outputs.version == 'true' ||
      needs.changes.outputs.citations == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/fed
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: apps/fed/package-lock.json

      - name: Install
        # --ignore-scripts: a PR gate installs the dependency tree of a branch anyone can open, so an
        # install hook here runs attacker-chosen code on the runner before any gate has looked at the
        # diff. It also keeps this install identical to deploy.yml's — the gate must exercise the same
        # tree the deploy ships, or "green here" stops meaning "safe there" (ADR-0021). No dep needs a
        # hook: the only one whose install hook actually runs is esbuild, and it only validates the
        # @esbuild/<platform> binary; the Playwright browser is installed by an explicit step.
        run: npm ci --ignore-scripts

  # Package-vulnerability gate (ADR-0021). HIGH/CRITICAL advisories in PRODUCTION deps block
  # (audit-ci.jsonc: skip-dev + high/critical); devDep advisories surface via Dependabot but never block
  # a static-site deploy. It queries a live advisory DB, so a new advisory can start blocking an
  # otherwise-unrelated PR — the checked-in allowlist (justification + expiry) is the relief valve.
  #
  # NO `if:`, DELIBERATELY, and it is the one job in this file for which a path filter is a CATEGORY
  # ERROR. Every other `code` job reads the diff: eslint, tsc, build-static and playwright all
  # open files the PR changed, so gating them on the paths that changed is exactly right. This job's
  # subject is `apps/fed/package-lock.json` CROSSED WITH A LIVE ADVISORY DATABASE, and the second half
  # moves with no commit at all — the comment directly above has said so since the job was written. So
  # "nothing under apps/** changed" is not evidence that there is nothing to find; it is evidence about
  # the wrong input.
  #
  # It ran on NONE of the five most recent `main` runs, including the merge base of #625, because every
  # one of them was a VERSION-only bump commit: `version` starts npm-ci and build-static but not `code`.
  # A path-filtered gate that was skipping is not a gate that was passing, and five greens on this axis
  # measured nothing. #625 tripped it only by accident, by touching apps/**.
  #
  # Dropping the `if:` rather than widening it to `test || version || citations` is not a shortcut: a
  # SKIPPED dependency skips the dependent (the npm-ci comment above relies on the same rule), so
  # `needs: npm-ci` alone already means "run whenever this tree is installed at all" — which is the
  # honest condition — and expresses it once instead of keeping a second copy of npm-ci's boolean in
  # sync by hand. `changes` leaves `needs:` along with the `if:` that was its only reader; the ordering
  # survives through npm-ci, which needs it.
  #
  # Provenance: measured on PR #625, whose npm-audit red was the FIRST reading this axis had produced in
  # five `main` runs. See that PR's gate verdict for the measurement as it was found.
  #
  # RESIDUAL, named because this does not close it: nothing here is SCHEDULED. A new advisory published
  # against a lockfile nobody touches is still invisible until the next PR opens, whatever it changes.
  # Closing that needs a `schedule:` trigger, a decision about which workflow owns it and who reads a
  # failure nobody caused — its own slice, and Dependabot alerts cover part of it today.
  npm-audit:
    needs: npm-ci
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/fed
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: apps/fed/package-lock.json
      - name: Install
        run: npm ci --ignore-scripts
      - name: Audit
        run: npm run audit

  eslint:
    needs: [changes, npm-ci]
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/fed
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: apps/fed/package-lock.json
      - name: Install
        run: npm ci --ignore-scripts
      - name: Lint
        run: npm run lint

  # `tsc --noEmit` runs a second time inside build-static's `npm run build`. Accepted duplication (it is
  # seconds, and `npm run build` stays self-sufficient for local use) — see the design doc's open
  # decision 2. The alternative is stripping it from the build script, which costs the local type check.
  tsc:
    needs: [changes, npm-ci]
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/fed
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: apps/fed/package-lock.json
      - name: Install
        run: npm ci --ignore-scripts
      - name: Typecheck
        run: npm run typecheck

  # Coverage >= 85% is a gate, as a GLOBAL AVERAGE (ADR-0018's 2026-07-29 amendment) — not a per-file
  # floor. The lcov report is handed to sonarqube-scan as an artifact, which is the ONLY reason that job
  # needs this one: Sonar re-reads the source itself, but it cannot recompute coverage.
  #
  # `citations` is ORed in (#456): the ADR citation gate lives in this suite and its subject is the
  # whole repository, so a citation typed into `iac/`, `.github/` or a root markdown file has to run
  # it. See the `citations` filter above for why that category gates this job and not `sonarqube-scan`.
  vitest:
    needs: [changes, npm-ci]
    if: needs.changes.outputs.test == 'true' || needs.changes.outputs.citations == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/fed
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: apps/fed/package-lock.json
      - name: Install
        run: npm ci --ignore-scripts
      - name: Test (coverage >= 85%)
        run: npm test
      - name: Upload coverage for the Sonar scan
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage
          path: apps/fed/coverage/lcov.info
          retention-days: 1
          if-no-files-found: error

  # Named after the script, not after `vite`: build:static runs `tsc --noEmit && vite build &&
  # gen-sitemap && prerender`. `vite-build` would name a quarter of it and hide the part that matters
  # most — the prerender launches Chromium and snapshots every route in both locales, which is what puts
  # the OG tags in the served HTML and prints /cv.pdf.
  #
  # Runs on a VERSION-only change too: the version is compiled into the bundle.
  build-static:
    needs: [changes, npm-ci]
    if: needs.changes.outputs.any == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/fed
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: apps/fed/package-lock.json
      - name: Install
        run: npm ci --ignore-scripts

      # BEFORE the build, not after: scripts/prerender.mjs launches headless chromium to snapshot each
      # route and emit /cv.pdf. On a fresh `npm ci` runner the browser does not exist yet.
      - name: Install Playwright browser (the prerender drives it)
        run: npx playwright install --with-deps chromium

      - name: Build (static — prerendered)
        # A TEST measurement id so the E2E can exercise the consent banner without touching the real GA
        # property — the Playwright suite blocks the googletagmanager request, so no hit leaves CI. Set
        # explicitly rather than inheriting build:static's own default: the gate should state the id it
        # tests with.
        env:
          VITE_GA_MEASUREMENT_ID: G-TEST00000000
        run: npm run build:static

      - name: Upload dist for the Playwright run
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dist
          path: apps/fed/dist
          retention-days: 1
          if-no-files-found: error

  # The full functional regression, against a `vite preview` of the artifact build-static just produced.
  # It NEVER runs on an e2e/**-only filter of its own, because it drives the build: the spec files are
  # inside apps/**, so the `code` filter already covers them.
  #
  # NOTE what this artifact is NOT: it cannot be promoted to production. The deploy rebuilds after the
  # version bump, so the shipped bundle differs from this one by the VERSION string. See deploy.yml.
  playwright:
    needs: [changes, build-static]
    if: needs.changes.outputs.any == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/fed
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: apps/fed/package-lock.json
      - name: Install
        run: npm ci --ignore-scripts

      # A SECOND browser download, on a second runner. This is the clearest cost of splitting the gate
      # into jobs: build-static already paid for one and cannot hand it over.
      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Download the built site
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dist
          path: apps/fed/dist

      # `:built` skips the rebuild `e2e:local` does — the artifact above IS the build, and a fresh
      # checkout cannot have a stale dist/. Locally the default script rebuilds first, because there
      # dist/ CAN be stale and a suite run against old output passes for the wrong reason.
      - name: E2E (Playwright against a preview of the build)
        run: npm run e2e:local:built

  # The one job that reads another repository WITHOUT A CREDENTIAL (#318, ADR-0043; narrowed by #345).
  #
  # It was "the ONE job that reads another repository" until #345 gave `deploy`'s `deploy-app` a second
  # checkout of the same plugin, to resolve its version for the /portfolio card. Uniqueness stopped being
  # the property that licenses a cross-repo read; being uncredentialed is what licenses THIS one, and
  # placement is what licenses that one (its checkout precedes `Configure AWS credentials` and deletes the
  # tree on every exit, so the foreign tree and the AWS role never coexist). Corrected here as well as in
  # .github/workflows/README.md because this comment is what a reader meets while looking at the job that
  # has no credential — the place the old sentence was most likely to be believed.
  #
  # /architecture publishes an inventory of the dev-loop harness — personas, hooks, command families —
  # whose source of truth is `tadeumendonca-skills`. The build never reads it: a committed manifest
  # (apps/fed/src/content/generated/harness.json) is what ships, exactly like adrs.json and diagrams.json,
  # so ADR-0002 and ADR-0004 hold and a clone of this repo alone still builds. THIS job is the only place
  # the two are compared, three ways — missing, orphaned, changed.
  #
  # It needs no npm install and no browser: the script is dependency-free Node reading two trees.
  #
  # WHY THE SECOND CHECKOUT IS SAFE, and this is the property the decision turns on: the plugin is a
  # PUBLIC repository, so `actions/checkout` needs no token, no secret and no `id-token`. This widens no
  # credential surface. Nothing here goes near deploy.yml's gate filter, whose outputs decide whether an
  # OIDC-credentialed job runs — that is ADR-0015 applied to a path filter, and the manifest already sits
  # under apps/fed/, which the `code` filter matches.
  #
  # WHAT THIS JOB CANNOT DO, said here because it is the accepted cost rather than a gap to fix later: it
  # is NOT self-triggering. The event that falsifies the manifest is a merge in the OTHER repo, and no
  # filter in this one can match it — unlike `docs/adr/**`, which was added to the `test` filter above for
  # exactly this problem and could be, because the authored source is a path here. So the red lands on the
  # next run here, usually an unrelated PR and an author who did not cause it. The mitigation is in the
  # failure message, which names the plugin, says it is not your change, and gives the fix.
  harness-drift:
    needs: changes
    if: needs.changes.outputs.harness == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Checkout the dev-loop plugin (public — no token)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: tedeuxx/tadeumendonca-skills
          path: .skills-checkout

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      # No `npm ci`: the script imports nothing outside node: builtins. Run directly rather than through
      # the npm script so the plugin path is an explicit argument instead of an inherited default.
      - name: Compare the published harness inventory against the plugin
        run: node apps/fed/scripts/check-harness-drift.mjs "$GITHUB_WORKSPACE/.skills-checkout"

  # Authoritative quality gate; `-Dsonar.qualitygate.wait=true` makes it blocking.
  #
  # It needs `vitest` and NOT `build-static`: sonar-project.properties sets `sonar.sources=src,scripts`
  # and `sonar.javascript.lcov.reportPaths=coverage/lcov.info`. dist/ is in neither, so waiting for the
  # build would serialise this job behind it for no input. (The design doc drew this edge conservatively
  # as needing both — the properties file answers it.)
  #
  # Skipped on Dependabot PRs: GitHub isolates Actions secrets from Dependabot-triggered runs, so
  # SONAR_TOKEN is empty there and the scan fails "not authorized", blocking every dep bump for an
  # infrastructural reason rather than a code one. The other gates still run.
  sonarqube-scan:
    needs: [changes, vitest]
    if: needs.changes.outputs.test == 'true' && github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        # Full history — SonarCloud uses git blame for new-code / authorship attribution.
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Download coverage
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: coverage
          path: apps/fed/coverage

      - name: SonarCloud scan (quality gate blocks)
        uses: SonarSource/sonarqube-scan-action@22918119ff8e1ca75a623e15c8296b6ea4fbe28f # v8.2.1
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io
        with:
          projectBaseDir: apps/fed
          args: -Dsonar.qualitygate.wait=true

  # ---------------------------------------------------------------------------------------------
  # THE REQUIRED CHECK. The name `build-test` is fixed by branch protection and must not change.
  #
  # `if: always()` is load-bearing twice over:
  #
  #  (1) A bare `needs:` aggregator is SKIPPED when any dependency fails — and a skipped required check
  #      satisfies branch protection. The monolith could not have this bug (one job, one status); the
  #      split introduces it, and this is where it is paid for. The job must RUN and decide.
  #  (2) On a PR that touches nothing here, every job above is skipped and this one still has to report,
  #      or the required check never arrives.
  #
  # `contains(needs.*.result, ...)` is used rather than naming each job: a job id with a hyphen is not
  # reachable through dot notation, and a list that has to be kept in sync with `needs:` is one more
  # place to forget.
  # ---------------------------------------------------------------------------------------------
  build-test:
    needs:
      [
        changes,
        npm-ci,
        npm-audit,
        eslint,
        tsc,
        vitest,
        build-static,
        playwright,
        harness-drift,
        sonarqube-scan,
      ]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Fail if any gate failed or was cancelled
        if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')
        run: |
          echo "::error::a gate job failed or was cancelled — see the jobs above"
          exit 1

      # A path-filtered gate that matched nothing reports PASS exactly like one that ran every check, and
      # that ambiguity is the second half of issue #79.
      #
      # `npm-audit` is NOT keyed on any of these booleans — it runs whenever npm-ci ran, i.e. on
      # `test || version || citations`. FOUR of the five branches below therefore name it, and only
      # `no gated changes` does not: that is the one shape where every category is false, so npm-ci is
      # skipped and npm-audit with it.
      #
      # THE CLAUDE.md-ONLY BRANCH IS THE TRAP, and it shipped saying the opposite for one review round.
      # `CLAUDE.md` is matched by the `citations` pattern `*.md` (picomatch, root-level only), and it is
      # the ONLY `harness` path not also covered by `test` — every other one sits under `apps/**`. So
      # this branch's entire domain is "CLAUDE.md and nothing else", and on that shape `citations` is
      # ALWAYS true: npm-ci starts, npm-audit runs, and so does vitest, which is the citation gate. A
      # CLAUDE.md-only PR is not the light-touch run this sentence used to describe. Measured on three
      # real pre-fix runs (34261407171, 34160893755, 34144393858): npm-ci success, three for three.
      #
      # These strings no longer rest on that reading being right. `app-workflow.test.mjs` DERIVES, from
      # these filter patterns through picomatch, which jobs run for a representative diff of each shape,
      # and asserts each notice agrees — so a filter edit that changes what runs reddens the notice that
      # describes it. It also asserts this ladder carries exactly the branches it knows about, so a new
      # one cannot escape unnoticed.
      #
      # Every combination below is distinguished, not just
      # a fixed count of cases — `code`/`test`/`harness`/`version` are independent booleans since the
      # `code`/`test`/`harness` filter split, so listing "N cases" here would just be one more count to
      # keep in sync by hand.
      - name: Report what was verified
        if: always()
        env:
          CODE: ${{ needs.changes.outputs.code }}
          TEST: ${{ needs.changes.outputs.test }}
          HARNESS: ${{ needs.changes.outputs.harness }}
          VERSION_ONLY: ${{ needs.changes.outputs.version }}
          CITATIONS: ${{ needs.changes.outputs.citations }}
          SONAR: ${{ needs.sonarqube-scan.result }}
        run: |
          set -euo pipefail
          # CITATIONS is in this condition for the reason this whole step exists (#79): `vitest` now
          # runs on it, so without it a `citations`-only PR would run the ADR citation gate and then
          # report "nothing was verified". A check that ran must never read like one that did not,
          # and the inverse is the same defect.
          if [ "$CODE" != "true" ] && [ "$TEST" != "true" ] && [ "$HARNESS" != "true" ] && [ "$VERSION_ONLY" != "true" ] && [ "$CITATIONS" != "true" ]; then
            echo "::notice::no gated changes (apps/**, packages/shared, iac/cloudfront-functions, docs/adr, CLAUDE.md, VERSION, iac/**, .github/**, root *.md) — nothing was verified"
            exit 0
          fi
          if [ "${{ job.status }}" != "success" ]; then
            echo "::notice::the app gate did NOT complete — a job failed, so the ones downstream of it never ran. Read the failing job, not this line."
            exit 0
          fi
          # Ordered AFTER the job.status check, like every other case below it: a branch that reports
          # what ran must not fire on a run where something failed.
          if [ "$CITATIONS" = "true" ] && [ "$CODE" != "true" ] && [ "$TEST" != "true" ] && [ "$HARNESS" != "true" ] && [ "$VERSION_ONLY" != "true" ]; then
            echo "::notice::citation-surface-only change (iac/**, .github/**, docs outside adr, root *.md) — npm-audit and vitest ran, the latter being the ADR citation gate; lint, typecheck, build, Playwright and Sonar had nothing to look at and did NOT run"
            exit 0
          fi
          if [ "$HARNESS" = "true" ] && [ "$CODE" != "true" ] && [ "$TEST" != "true" ] && [ "$VERSION_ONLY" != "true" ]; then
            echo "::notice::CLAUDE.md-only change — npm-audit, vitest and harness-drift ran (CLAUDE.md is also in the citations filter, so npm-ci and the citation gate start); lint, typecheck, build, E2E and Sonar had nothing to look at and did NOT run"
            exit 0
          fi
          if [ "$TEST" = "true" ] && [ "$CODE" != "true" ] && [ "$VERSION_ONLY" != "true" ]; then
            echo "::notice::docs/adr or deploy.yml-only change — npm-audit, vitest and sonarqube-scan ran; lint, typecheck, build and Playwright had nothing to look at and did NOT run"
            exit 0
          fi
          if [ "$CODE" != "true" ] && [ "$VERSION_ONLY" = "true" ]; then
            echo "::notice::VERSION-only change — npm-audit, build-static and Playwright ran; lint, typecheck, tests and Sonar had nothing to look at and did NOT run"
            exit 0
          fi
          if [ "$HARNESS" = "true" ]; then
            echo "::notice::the app gate ran — npm-audit, eslint, tsc, vitest (coverage >=85%), build-static, playwright, harness-drift; sonarqube-scan: ${SONAR}"
          else
            echo "::notice::the app gate ran — npm-audit, eslint, tsc, vitest (coverage >=85%), build-static, playwright; sonarqube-scan: ${SONAR}"
          fi
