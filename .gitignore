# deps
node_modules/

# builds
dist/
dist-digest/
build/
*.tsbuildinfo

# test/coverage
coverage/
playwright-report/
test-results/

# terraform
**/.terraform/
**/.terraform.lock.hcl
iac/builds/
*.tfstate
*.tfstate.*
crash.log

# env / secrets
.env
.env.*
!.env.example

# personal-brand strategy layer — private working notes for the repositioning
# initiative (positioning, canonical copy, sync playbook, backlog). Local only,
# never published: this repo is public.
.brand/

# scratch — the ONE place for throwaway files (PR bodies, commit messages). Ignored rather than
# absent, so a scratch write lands somewhere NAMED instead of somewhere random: the failure this
# replaces was an agent leaving a bundled `dist-old-qa/` at a repo path nothing ignored, which
# dirtied `git status` and broke local lint with 1480 errors. Emptied at the start of every new
# session by the plugin's `session-scratch` hook — see CLAUDE.md for what does and does not belong.
# ANCHORED with a leading slash, and the anchor is the whole point. Without it the pattern matches
# `.scratch/` at ANY depth — including `apps/fed/.scratch/`, which this same commit retires loudly and
# whose `CLAUDE.md` entry says is deliberately NOT ignored so that writing there is visible rather than
# silent. An unanchored pattern made that sentence false in the commit that wrote it. The hook sweeps
# `<root>/.scratch` only, so an ignored-but-unswept directory is the worst of both: invisible to
# `git status` and immortal.
/.scratch/

# editor / os
.DS_Store
*.log

# Claude Code — personal/local overrides (e.g. autoMemoryDirectory). Never committed.
.claude/settings.local.json
