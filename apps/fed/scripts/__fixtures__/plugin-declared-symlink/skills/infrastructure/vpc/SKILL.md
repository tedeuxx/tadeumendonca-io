---
description: fixture skill
---

The one skill this fixture really holds. `skills/elsewhere` is created by the test as a symlink out
of this tree, and removed again — it is not committed, so a checkout on a filesystem without symlinks
is unaffected.
