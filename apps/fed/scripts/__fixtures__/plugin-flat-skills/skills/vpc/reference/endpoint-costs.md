A supporting file, in a subdirectory of the skill.

It is here as a FIXTURE ASSERTION, not as content: the new capability the `skills/` form buys is exactly
this — a skill may carry its own directory of supporting material. So the flatness invariant applies to
`skills/` itself and stops there. If a future guard starts walking below a skill directory, this file is
what turns it red.
