A markdown file directly under `skills/`. Claude Code cannot load this as a skill — the form is a
directory holding `SKILL.md`. Counting it would publish a capability that does not exist; skipping it
would under-report the figure. Both are refused.
