---
description: fixture skill
---

The one skill this fixture holds. `skills/infrastructure/VPC` is the same directory on a
case-insensitive filesystem and a different one on a case-sensitive filesystem.
